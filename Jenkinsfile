pipeline {
    agent any
    parameters {
        string defaultValue: 'latest', description: 'Application Version Tag', name: 'Version', trim: true
    }
    stages {
        stage {
            script {
                echo "hellllo"
            }
        }
    }
}